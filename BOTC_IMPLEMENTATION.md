# 血染钟楼（Blood on the Clocktower）后端实现

## 概述

本项目成功实现了血染钟楼游戏的完整后端逻辑，支持所有三个官方剧本：
- **Trouble Brewing** (初学者版本)
- **Bad Moon Rising** (中级版本) 
- **Sects & Violets** (中级版本)

## 技术架构

### 核心文件结构

```
backend/src/
├── workers/
│   └── botcWorker.ts           # 主要游戏逻辑Worker
├── utils/
│   ├── botcTypes.ts            # 类型定义
│   ├── botcData.ts             # 游戏数据（角色、版本）
│   ├── botcUtils.ts            # 工具函数
│   └── botcSkills.ts           # 角色技能处理器
```

### 主要组件

#### 1. BOTCWorker (botcWorker.ts)
- 继承自 `BaseGameWorker`
- 实现完整的游戏生命周期管理
- 支持房间管理、玩家管理、游戏状态控制
- 处理投票系统、夜晚行动、胜负判定

#### 2. 类型系统 (botcTypes.ts)
- `Team`: 角色队伍枚举（村民、外来者、爪牙、恶魔、旅行者）
- `GamePhase`: 游戏阶段枚举（设置、首夜、白天、夜晚、结束）
- `Role`: 角色接口定义
- `GamePlayer`: 游戏玩家状态
- `GameState`: 完整游戏状态
- 支持5-15人游戏的玩家配置

#### 3. 游戏数据 (botcData.ts)
- 三个官方剧本的完整角色数据
- 中文本地化的角色名称和能力描述
- 夜晚行动顺序定义
- 版本信息和角色筛选功能

#### 4. 工具函数 (botcUtils.ts)
- 角色分配算法
- 游戏状态管理
- 胜负条件检查
- 投票计算
- 邻居关系处理

#### 5. 技能处理器 (botcSkills.ts)
- 专门处理复杂的角色能力
- 首夜信息技能（洗衣妇、图书管理员、调查员等）
- 夜晚行动技能（投毒者、僧侣、恶魔等）
- 技能效果和互动处理

## 游戏特性

### 基础功能
- ✅ 房间创建和配置
- ✅ 玩家加入/离开/踢出
- ✅ 说书人权限管理
- ✅ 角色自动分配
- ✅ 游戏阶段转换

### 投票系统
- ✅ 提名机制
- ✅ 投票收集和计算
- ✅ 处决判定
- ✅ 投票权限管理

### 夜晚阶段
- ✅ 夜晚行动顺序
- ✅ 角色能力处理
- ✅ 说书人界面
- ✅ 行动结果处理

### 胜负判定
- ✅ 善良阵营胜利条件（恶魔死亡）
- ✅ 邪恶阵营胜利条件（人数优势/2人终局）
- ✅ 特殊胜利条件支持

### 角色能力
- ✅ 信息类技能（洗衣妇、图书管理员、调查员、厨师等）
- ✅ 保护类技能（僧侣、士兵）
- ✅ 杀伤类技能（恶魔击杀、杀手）
- ✅ 状态类技能（投毒、醉酒、保护）
- ✅ 复杂互动（间谍、红颜、管家等）

## 支持的角色

### Trouble Brewing (13个核心角色)
**村民**: 洗衣妇、图书管理员、调查员、厨师、共情者、占卜师、殡仪员、僧侣、乌鸦饲养员、处女、杀手、士兵、镇长

**外来者**: 管家、酒鬼、隐士、圣徒

**爪牙**: 投毒者、间谍、红颜、男爵

**恶魔**: 小恶魔

### Bad Moon Rising (部分实现)
- 祖母、水手、侍女、驱魔师
- 暴徒、疯子
- 教父
- 僵尸、普卡

### Sects & Violets (部分实现)  
- 钟表匠、梦想家、哲学家
- 笨蛋、甜心
- 邪恶双子、女巫
- 彊尸、维格莫提斯、诺达希、沃托克斯

## 技术特点

### Worker 架构
- 使用 Node.js Worker Threads 实现游戏隔离
- 每个房间运行在独立的 Worker 中
- 支持并发多房间游戏

### 类型安全
- 完整的 TypeScript 类型定义
- 编译时类型检查
- 接口契约保证

### 可扩展性
- 模块化的角色技能系统
- 易于添加新角色和版本
- 灵活的游戏规则配置

### 本地化
- 完整的中文翻译
- 角色名称和能力描述本地化
- 用户友好的界面文本

## 使用方法

### 创建游戏房间
```typescript
// 游戏配置
const config: GameConfig = {
  edition: 'tb',              // 版本选择
  storytellerId: 'user123',   // 说书人ID
  allowSpectators: true,      // 允许观众
  maxPlayers: 15,            // 最大玩家数
  enableTimers: true,        // 启用计时器
  dayTimer: 300,             // 白天时间（秒）
  nightTimer: 180,           // 夜晚时间（秒）
  votingTimer: 60            // 投票时间（秒）
};
```

### 游戏行动
```typescript
// 提名
gameAction(playerId, 'nominate', { nomineeId: 'target123' });

// 投票
gameAction(playerId, 'vote', { vote: 'for' });

// 夜晚行动
gameAction(playerId, 'nightAction', { 
  actionType: 'ability', 
  targets: ['target123'] 
});
```

## 后续扩展

### 计划功能
- [ ] 完整的三个剧本所有角色实现
- [ ] 旅行者角色支持
- [ ] 自定义剧本支持
- [ ] 游戏录像和回放
- [ ] 统计系统

### 技术优化
- [ ] 性能优化
- [ ] 内存管理
- [ ] 错误处理完善
- [ ] 单元测试

## 总结

血染钟楼后端实现已经完成了核心游戏逻辑，支持完整的游戏流程和三个官方剧本的主要角色。系统架构清晰，代码结构良好，易于维护和扩展。所有代码都遵循了项目的命名规范，使用 `botc` 前缀进行区分。 